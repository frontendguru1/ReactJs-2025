const Header = ({ title }) => {
  // console.log(props);

  // const props = {
  //   name: "abc",
  //   title: "Login new"
  // }

  return <h1 className="header">{title}</h1>;
};

export default Header;
